import bezier from 'dummy/utils/bezier';
import { module, test } from 'qunit';

module('Unit | Utility | bezier');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = bezier();
  assert.ok(result);
});
