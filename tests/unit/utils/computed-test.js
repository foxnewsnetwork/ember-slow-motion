import computed from 'dummy/utils/computed';
import { module, test } from 'qunit';

module('Unit | Utility | computed');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = computed();
  assert.ok(result);
});
